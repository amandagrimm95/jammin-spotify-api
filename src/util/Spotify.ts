let accessToken: number;

export const Spoitfy: object = {
    getAccessToken() {
        if (accessToken !== undefined) {
            return accessToken;
        } else {
            
        }
    }
};

